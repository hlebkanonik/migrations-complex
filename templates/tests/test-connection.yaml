apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "migrations-complex.fullname" . }}-test-connection"
  labels:
    {{- include "migrations-complex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "migrations-complex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

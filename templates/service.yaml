apiVersion: v1
kind: Service
metadata:
  name: {{ include "migrations-complex.fullname" . }}
  labels:
    {{- include "migrations-complex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "migrations-complex.selectorLabels" . | nindent 4 }}
